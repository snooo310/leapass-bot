import { Client } from "@notionhq/client";

const notion = new Client({ auth: process.env.NOTION_API_KEY });

// フォールバック用TIPS（Notion接続前でも動作するよう内蔵）
const FALLBACK_TIPS = `【1分以上迷ったら次へ行く】
カテゴリ: 時間管理
内容: TOEICは時間との勝負。1問に1分以上かけると後半が崩壊します。迷ったらマークして次へ。見直し時間を確保することが高スコアへの近道です。

---

【Part5を爆速で解く「秒殺消去法」】
カテゴリ: Part5
対象スコア帯: 全スコア
内容: Part5の目標は1問20〜25秒。①品詞チェック（選択肢の品詞を確認）②空所の前後だけ読む③文法ルールで消去。でる1000問でパターンを体に叩き込みましょう。

---

【リスニングが聞き取れない原因と対策】
カテゴリ: Part4
対象スコア帯: 全スコア
内容: 「聞き取れない」には必ず原因があります。①単語力不足→金フレでインプット ②音声変化未対応（リンキング・フラッピング）→シャドーイングで体に染み込ませる ③処理速度不足→1.5〜2倍速訓練。原因を特定することが最重要。

---

【Part7で時間が足りなくなる人の解決策】
カテゴリ: Part7
対象スコア帯: 700点台
内容: 推奨時間配分：Part5(30問)10〜12分、Part6(16問)8分、Part7(54問)残り54分。スキミングのコツ：設問を先読みして「何を探すか」把握→本文を読む。ダブルパッセージは「どの文書に答えがあるか」を先に見極める。

---

【TOEIC 900点取得までの期間の目安】
カテゴリ: 全般
対象スコア帯: 600点台
内容: 毎日1〜2時間継続した場合の目安：600点台→900点：6〜12ヶ月、700点台→900点：3〜6ヶ月。スタート時の英語力や学習の質によって個人差があります。焦らず着実に積み上げることが大切。

---

【900点突破に向けて重点的に伸ばすべきポイント】
カテゴリ: 全般
対象スコア帯: 800点台
内容: ①Part4（長めのトーク）の集中演習：高スコア帯で差がつくパート ②Part5（文法）：でる1000問で頻出パターンを完全習得 ③Part6＆7：問題数が多く得点の伸びしろが大きい。経験値（問題数）を積むことが最優先。

---

【勉強が続かない人への処方箋：3の魔の法則】
カテゴリ: メンタル
対象スコア帯: 全スコア
内容: 続くために必要な3つの仕組み：①日報（毎日5行でOK）②相談相手（詰まったとき聞ける人1人）③仲間（一緒に頑張る存在）。「3の魔の法則」：スタートから3日・3週間・3ヶ月が挫折しやすいポイント。このタイミングで仲間や講師に連絡する習慣を。

---

【TOEIC学習におすすめの問題集・アプリ】
カテゴリ: 教材
対象スコア帯: 全スコア
内容: アプリ→abceed（AI分析で弱点を自動抽出）。問題集→でる1000問（Part5特化・解説が丁寧）、公式問題集（本番と最も近い形式）。「量より質」ではなく「1冊を何周やったか」が大事。浮気せず1冊を完璧に仕上げること。

---

【目的別おすすめ参考書ガイド】
カテゴリ: 教材
対象スコア帯: 全スコア
内容: 単語→金のフレーズ（金フレ）：TOEIC頻出600語、フレーズで覚えるので定着率高い。文法→でる1000問。総合→公式問題集（最新版）。⚠️同じ本を5周した人が10冊1周した人より圧倒的に伸びます。

---

【模試の復習を最大効果にする4ステップ】
カテゴリ: 全般
対象スコア帯: 全スコア
内容: ①タグ付け：確信○・迷い△・わからない×に仕分け ②採点：△と×に集中 ③分類：ミスの原因を「単語」「文法」「読み間違い」「時間不足」に分類 ④ディクテーション＆解説精読。模試1回の復習に模試を解く時間の2倍をかけること。

---

【シャドーイング教材の選び方と実施法】
カテゴリ: 全般
対象スコア帯: 全スコア
内容: 教材条件：①スクリプトがついていること ②自分が7〜8割聞き取れるレベル。コスパ最強→TOEIC公式問題集のPart3・4（本番と同じ音声・スピード）。手順：スクリプトなし→スクリプト見ながら音読→シャドーイング→スクリプトなしシャドーイング。1日1トラック毎日続けるだけで3ヶ月後に大きな差が出る。

---

【700点台から800点台へ：突破のための3本柱】
カテゴリ: 全般
対象スコア帯: 700点台
内容: 700→800点突破には約200時間の追加学習が目安。①シャドーイング継続（毎日10〜15分、公式問題集Part3・4で実施）②演習経験値（公式問題集を最低3回分、間違い問題を徹底解析）③時間配分の最適化（自分だけのタイムテーブルを作って本番形式で練習）。

---

【TOEICスコアは就職・転職に本当に役立つのか】
カテゴリ: 全般
対象スコア帯: 全スコア
内容: 結論：役立ちます。採用選考でTOEICを参考にする企業は約40%。900点台取得者は受験者全体の上位4%。外資系・グローバル企業では700〜800点が応募要件になることも多い。転職では「英語力の証明」として最も汎用性が高い資格。

---

【LeaPASSのサービス内容について】
カテゴリ: 全般
対象スコア帯: 全スコア
内容: TOEIC900点突破に特化したパーソナルコーチング。料金¥98,000（買い切り・分割相談可）。内容：①個別学習計画作成 ②月2回オンライン面談 ③無制限チャットサポート ④毎日の日報フィードバック ⑤オンライン学習コンテンツ ⑥受講生コミュニティ ⑦キャリア相談サポート。詳しくはInstagram @reo_toeiceng のDMへ。`;

// Notionデータベースから全TIPSを取得してテキストにまとめる
// スキーマ: TIPS(title), 内容(text), カテゴリ(select), スコア帯(multi_select)
export async function fetchTips() {
  const databaseId = process.env.NOTION_DATABASE_ID;

  // Notion接続を試みる。失敗した場合はフォールバックTIPSを返す
  try {
    const tips = [];
    let cursor = undefined;

    while (true) {
      const response = await notion.databases.query({
        database_id: databaseId,
        start_cursor: cursor,
        page_size: 100,
      });

      for (const page of response.results) {
        const props = page.properties;

        const title = props["TIPS"]?.title?.map((t) => t.plain_text).join("") ?? "";
        const content = props["内容"]?.rich_text?.map((t) => t.plain_text).join("") ?? "";
        const category = props["カテゴリ"]?.select?.name ?? "";
        const scoreBands = props["スコア帯"]?.multi_select?.map((s) => s.name).join(", ") ?? "";

        if (!title) continue;

        const lines = [`【${title}】`];
        if (category) lines.push(`カテゴリ: ${category}`);
        if (scoreBands) lines.push(`対象スコア帯: ${scoreBands}`);
        if (content) lines.push(`内容: ${content}`);

        tips.push(lines.join("\n"));
      }

      if (!response.has_more) break;
      cursor = response.next_cursor;
    }

    // Notionから取得できた場合はそれを返す
    if (tips.length > 0) return tips.join("\n\n---\n\n");

    // TIPSが0件の場合はフォールバック
    console.log("[notion] 0 tips found, using fallback");
    return FALLBACK_TIPS;
  } catch (err) {
    // Notion接続エラー時はフォールバックTIPSを使用
    console.error("[notion] fetchTips error, using fallback:", err.message);
    return FALLBACK_TIPS;
  }
}
